development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") 
  
  channel_prefix: jcars-app_production
  ssl_params:
  verify_mode: OpenSSL::SSL::VERIFY_NONE