<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "BulinAutoSales" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="turbo-cache-control" content="no-cache">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
     <!-- Add Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
   <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="<%= asset_path('bulin_sales.jpg')%>?v=1" type="image/jpeg">
    <link rel="apple-touch-icon" href="/icon.png">
      <%= stylesheet_link_tag 'trix' %>
      <%= javascript_include_tag 'trix' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class ="bg-light">
     
  <header>
    <div class="button-container">
    <%= link_to "Home", root_path, class: 'home-button button link-button cool-text' %>
    </div>
    <!-- Display Flash Messages -->
    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <!-- User Authentication Links -->
    <div class="devise-links">
    <% if user_signed_in? %>
        User <%= current_user.email %> 
        <div class= "button-container">
        <%= link_to "Edit Profile", edit_user_registration_path ,class: 'button link-button'  %> 
        <%= link_to "Sign Out", destroy_user_session_path,class: 'button link-button', method: :delete %>
        </div>
    <% else %>
        <div class= "button-container">
        <%= link_to "Sign In", new_user_session_path, class: 'button link-button' %> 
        <%= link_to "Sign up", new_user_registration_path,  class: "button link-button" %>
        </div>
    <% end %>
    </div>
  </header>
    <!-- Main Content -->
    <div class= "container">
    <%= yield %>
    </div>
  </body>
</html>
